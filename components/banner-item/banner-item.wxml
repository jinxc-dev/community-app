
<view class="banner-container">
    <image src="{{uploadPath + info.banner_image}}" class="banner-image" />
    <view class="banner-context">
        <view class="description">{{info.tag_line}}</view>
        <view class="date">{{info.posted_on}}</view>
    </view>
</view>