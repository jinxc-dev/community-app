<view class="page">
    <view class="title">
        {{info.title}}
    </view>
    <view class="context">
        {{communityName}}
        <text style="color:#888">{{info.date}}</text>
    </view>
    <image src="{{info.image}}" class="banner-image" bindtap="chooseImage" aspectFill/>
</view>
<!-- <view class="page-body">
    <view class="page-section page-section-gap">
        <web-view src="{{info.link}}"></web-view>
    </view>
</view> -->
